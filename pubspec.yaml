name: release_notes_dialog
description: An easy to use release notes dialog. Customizable to fit your apps design.
version: 2.0.0
author: Jakob Mick<info@jakobmick.de>
homepage: https://github.com/JakobMick/release_notes_dialog

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
